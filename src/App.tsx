import React from "react";

const App = () => {
  console.log("✅ React está rodando!");
  return (
    <div style={{ padding: "100px", textAlign: "center", fontSize: "2rem" }}>
      🧠 <strong>React está funcionando!</strong>
    </div>
  );
};

export default App;
